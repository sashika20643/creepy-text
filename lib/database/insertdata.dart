import 'package:flutter/material.dart';

class FDatabase extends StatefulWidget {
  const FDatabase({Key? key}) : super(key: key);

  @override
  State<FDatabase> createState() => _FDatabaseState();
}

class _FDatabaseState extends State<FDatabase> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
